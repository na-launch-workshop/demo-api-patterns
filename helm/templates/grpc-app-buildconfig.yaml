{{- if and (index .Values "grpc-app" "enabled") (index .Values "grpc-app" "buildConfig" "enabled") }}
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: {{ include "demo-apipatterns-chart.grpcAppFullname" . }}
  labels:
    {{- include "demo-apipatterns-chart.labels" . | nindent 4 }}
spec:
  runPolicy: {{ .Values.buildConfig.runPolicy | default "Serial" }}
  source:
    type: Git
    git:
      uri: {{ .Values.buildConfig.git.uri | quote }}
      {{- with .Values.buildConfig.git.ref }}
      ref: {{ . | quote }}
      {{- end }}
    {{- with (index .Values "grpc-app" "buildConfig" "contextDir") }}
    contextDir: {{ . | quote }}
    {{- end }}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: {{ .Values.buildConfig.dockerfilePath | default "Dockerfile" | quote }}
      {{- if .Values.buildConfig.env }}
      env:
        {{- toYaml .Values.buildConfig.env | nindent 8 }}
      {{- end }}
  output:
    to:
      kind: ImageStreamTag
      name: {{ printf "%s:%s" (index .Values "grpc-app" "imageStream" "name") (index .Values "grpc-app" "imageStream" "tag") | quote }}
  {{- if or .Values.buildConfig.triggers.configChange .Values.buildConfig.triggers.imageChange }}
  triggers:
    {{- if .Values.buildConfig.triggers.configChange }}
    - type: ConfigChange
    {{- end }}
    {{- if .Values.buildConfig.triggers.imageChange }}
    - type: ImageChange
    {{- end }}
  {{- end }}
  {{- with .Values.buildConfig.resources }}
  resources:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
