{{- if (index .Values "grpc-app" "enabled") }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "demo-apipatterns-chart.grpcAppFullname" . }}
  labels:
    {{- include "demo-apipatterns-chart.labels" . | nindent 4 }}
  annotations:
    image.openshift.io/triggers: >-
      [{"from":{"kind":"ImageStreamTag","name":"{{ index .Values "grpc-app" "imageStream" "name" }}:{{ index .Values "grpc-app" "imageStream" "tag" }}","namespace":"{{ .Release.Namespace }}"},"fieldPath":"spec.template.spec.containers[?(@.name=='grpc-app')].image"}]
    argocd.argoproj.io/ignore-differences: |-
      jsonPointers:
        - /spec/template/spec/containers/0/image
spec:
  replicas: {{ .Values.replicaCount | default 1 }}
  selector:
    matchLabels:
      app: {{ include "demo-apipatterns-chart.grpcAppFullname" . | quote }}
  template:
    metadata:
      labels:
        app: {{ include "demo-apipatterns-chart.grpcAppFullname" . | quote }}
    spec:
      containers:
        - name: grpc-app
          image: {{ include "demo-apipatterns-chart.grpcAppImage" . | quote }}
          imagePullPolicy: {{ index .Values "grpc-app" "imagePullPolicy" | default "IfNotPresent" }}
          ports:
            - containerPort: {{ index .Values "grpc-app" "service" "port" }}
              name: grpc
          env:
            {{- include "demo-apipatterns-chart.appEnv" . | nindent 12 }}
          {{- if .Values.sqlite.enabled }}
          volumeMounts:
            - name: sqlite-data
              mountPath: {{ .Values.sqlite.mountPath | quote }}
          {{- end }}
          {{- with (index .Values "grpc-app" "resources") }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.sqlite.enabled }}
      volumes:
        - name: sqlite-data
{{- if .Values.sqlite.persistent }}
          persistentVolumeClaim:
            claimName: {{ include "demo-apipatterns-chart.sqliteFullname" . }}
{{- else }}
          emptyDir: {}
{{- end }}
      {{- end }}
      {{- with (index .Values "grpc-app" "nodeSelector") }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (index .Values "grpc-app" "affinity") }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (index .Values "grpc-app" "tolerations") }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
