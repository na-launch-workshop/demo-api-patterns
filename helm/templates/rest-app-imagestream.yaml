{{- if (index .Values "rest-app" "enabled") }}
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: {{ index .Values "rest-app" "imageStream" "name" }}
  labels:
    {{- include "demo-apipatterns-chart.labels" . | nindent 4 }}
spec:
  lookupPolicy:
    local: true
{{- end }}
